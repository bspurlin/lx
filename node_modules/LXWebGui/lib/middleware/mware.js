// Note that the order determines what order the middleware is applied
var mware = { 
	multer: require('./mulware') 
}; 

module.exports = function(app) { 
	for(var item in mware) { 
		if(mware.hasOwnProperty(item)) { 
			mware[item](app); 
		}
	}
}; 
